def xor(condition_1, condition_2):
    return (condition_1 and not condition_2) or (condition_2 and not condition_1)

def grid_size(screen_x: int, screen_y: int, x_size: int, y_size: int) -> tuple:
    """Calculate the size of a sqaure on the grid."""
    spacing_x = x_size / 50
    

    


    return
